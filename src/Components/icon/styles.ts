import { createUseStyles } from "react-jss";

export default createUseStyles({
  iconText: {
    display: 'flex',
    alignItems: 'center',
    marginTop: '2%',
    marginRight: '2%'
  },
  icon: {
    marginRight: 5,
  }
});
